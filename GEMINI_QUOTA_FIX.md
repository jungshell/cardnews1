# ⚠️ Gemini API 사용량 제한 해결 가이드

## 현재 상황

**오류 메시지:**
```
[Gemini HTTP 오류] 429
Quota exceeded for metric: generativelanguage.googleapis.com/generate_content_free_tier_requests
limit: 20, model: gemini-2.5-flash
Please retry in 34.235458584s
```

**의미**: 무료 플랜의 일일 요청 수 제한(20회)을 초과했습니다.

---

## 해결 방법

### 방법 1: 내일까지 기다리기 (가장 간단)

**무료 플랜 제한:**
- 일일 20회 요청 제한
- 자정(UTC 기준)에 리셋됨
- 한국 시간으로는 오전 9시에 리셋

**조치:**
- 내일 오전 9시 이후 다시 시도
- 또는 오늘은 사용하지 않기

---

### 방법 2: 여러 API 키 사용 (권장)

**장점:**
- 여러 API 키를 번갈아 사용하여 제한 회피
- 각 키마다 20회씩 사용 가능

**설정 방법:**

1. **Google AI Studio에서 추가 API 키 생성**
   - https://aistudio.google.com/app/apikey 접속
   - "Create API Key" 클릭하여 2-3개 추가 생성

2. **Streamlit Cloud Secrets에 추가**
   - Streamlit Cloud → Settings → Secrets
   - 다음 형식으로 추가:
   ```toml
   GEMINI_API_KEY = "첫번째_키"
   GEMINI_API_KEY_2 = "두번째_키"
   GEMINI_API_KEY_3 = "세번째_키"
   ```

3. **코드 수정** (API 키를 순환 사용하도록)
   - `gemini_api.py` 파일 수정 필요
   - 여러 키 중 사용 가능한 키 선택

---

### 방법 3: 유료 플랜으로 업그레이드

**Google AI Studio 유료 플랜:**
- 더 높은 사용량 제한
- 월 구독 또는 사용량 기반 결제
- https://ai.google.dev/pricing 참고

**장점:**
- 일일 제한 없음 (또는 매우 높은 제한)
- 더 빠른 응답 속도
- 우선 지원

---

### 방법 4: 요청 빈도 줄이기

**현재 사용 패턴:**
- 매번 카드뉴스 생성 시 API 호출
- 원문 요약도 API 호출

**개선 방법:**
- 캐시 활용 (이미 구현됨)
- 불필요한 요청 줄이기
- 사용자가 정말 필요할 때만 생성

---

## 즉시 해결 (임시)

### 지금 당장 사용해야 하는 경우

1. **다른 Google 계정으로 새 API 키 생성**
   - 새 Google 계정 생성
   - Google AI Studio에서 새 API 키 생성
   - Streamlit Cloud Secrets에 업데이트

2. **34초 후 재시도** (하지만 일일 제한이면 실패)
   - 코드에 재시도 로직이 있지만
   - 일일 제한이면 재시도해도 실패

---

## 예방 방법

### 1. 사용량 모니터링

**Google AI Studio에서 확인:**
- https://aistudio.google.com/app/apikey
- API 키별 사용량 확인
- 일일/월일 제한 확인

### 2. 캐시 활용

**이미 구현됨:**
- 생성된 카드뉴스는 캐시에 저장
- 같은 기사는 다시 생성하지 않음
- "새로 생성" 버튼으로만 재생성

**추가 개선 가능:**
- 원문 요약도 더 적극적으로 캐시
- 사용자가 요청할 때만 생성

### 3. 여러 API 키 순환 사용

**코드 개선 필요:**
- 여러 API 키를 리스트로 관리
- 키별 사용량 추적
- 제한에 걸린 키는 자동으로 다른 키 사용

---

## 현재 상태 확인

### 오늘 사용량
- 무료 플랜: 20회 제한 초과
- 내일 오전 9시(한국 시간)에 리셋

### 다음 단계
1. **내일까지 기다리기** (가장 간단)
2. **또는 여러 API 키 준비** (장기적 해결)

---

## 코드 개선 제안

현재 코드는 단일 API 키만 사용합니다. 여러 키를 순환 사용하도록 개선할 수 있습니다.

**개선 사항:**
- 여러 API 키 지원
- 키별 사용량 추적
- 자동 키 전환

필요하시면 코드 개선을 도와드리겠습니다!

---

## 요약

**문제**: 무료 플랜 일일 20회 제한 초과
**해결**: 
1. 내일까지 기다리기 (가장 간단)
2. 여러 API 키 사용 (권장)
3. 유료 플랜 업그레이드

**지금 할 일**: 내일 오전 9시 이후 다시 시도하거나, 새 API 키를 준비하세요!
